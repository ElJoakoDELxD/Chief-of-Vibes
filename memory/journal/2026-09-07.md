---
updated: 07-09-2026
---

# 07-09-2026

## Done

- **1.65.0 landed.** Pull request #77 merged as `8014ab7`. `tools/test-now.sh` pinned the literal
  `-04` for `America/Santiago`, which is correct in winter and wrong under daylight saving. Chile
  moved its clocks on 06-09-2026 and the bench went red with `tools/now.sh` correct throughout.
  The offset now resolves at run time from `date`. Guard run 106 green, then `tools/sync.sh` onto
  this branch. Both sides read 1.65.0, verified by reading each.
- **1.66.0 landed and the branch list was swept.** Pull request #78 merged as `f11afa4`:
  `tools/sweep-branches.sh`, ten bench cases against a real bare repository, and a dispatch
  workflow, because a cloud session's git proxy refuses a ref deletion whatever branch is checked
  out. Dispatched in `report` mode, log read, then `delete`. **Twenty branches became seven** —
  fourteen deleted, six kept — verified against `git ls-remote` rather than the job's own count.
  The four survivors carry commits `main` does not have.
- **1.67.0 landed: the sensors that were blind.** Pull request #79 merged as `9ab70b5`, guard run
  108 green, synced, both sides read 1.67.0. `candidates.sh` had reported one item waiting and it
  was prose naming its own tag, while eleven real candidates carried none; the colon is now
  required and an untagged candidate is itself reported. `hygiene.sh` gained the two-vault check
  and it fires on this repository today, naming `Chief-of-Vibes-Agent` and `Custodian`. Release A
  of three.
- **1.68.0 landed: identity moves to the post.** Pull request #80 merged as `e4d4696`, guard run
  109 green, synced, both sides read 1.68.0. `.canon` left `main` and was re-added on this branch,
  where custody now lives. The guard split along whether a question needs identity: paths stay on
  the pull request, and the version question is asked by the post from its own branch — exercised
  for the first time on #80 itself, reporting 1.67.0 to 1.68.0 while CI correctly said the
  question was not asked there. §6 gained the membership test that was written nowhere.
  Release B of three.
- **1.69.0 landed: an agent holds a post, and a post authorizes functions.** Pull request #81
  merged as `48e4f37`, guard run 110 green, synced, both sides read 1.69.0. `posts/` and
  `functions/` are on `main` as the catalogue, `posts:` is in this vault's frontmatter, and the
  header gained the agent and the post over function. `steward` is what every copy's agent holds
  by default, which is the answer §6 never gave to *whose job is a copy's main*. Release C of
  three, and the plan is complete.
- **1.70.0 landed: the form stays and the filling moves.** Pull request #82 merged as `d3d46b1`,
  guard run 111 green, synced, both sides read 1.70.0. The catalogue shipped in 1.69.0 **was
  filled**: every post and function carried this repository's dates, pull request numbers and
  history, inherited by every copy and describing a repository none of them is. `verified:` keeps
  its job and loses its account, and the account moved to `memory/posts/custodian.md`. §5 states
  the rule and §6 says a shipped form found filled is repaired rather than deleted.
- **1.71.0 and 1.72.0 landed, and the second vault is retired.** #83 as `ffcc193` gave the sweep a
  retire mode that proves a fold by content; `Custodian` went through it, seven files kept byte
  for byte and the two merged by hand named. Twenty branches this morning, six now. #84 as
  `13e8071` built the model gate. Both synced, both sides at 1.72.0, 17 benches green.
- **The Principal decided the model question.** Opus holds the custodian post, and only Opus
  custodies the blueprint's `main`. Recorded on the `models:` line of
  `memory/posts/custodian.md`, where the list belongs, and it supersedes the 14-08 approval of
  sonnet for the scheduled guard — the guard is a function of this post, so it runs on Opus too
  unless the Principal says otherwise.
- **The custodian role written as a charter**,
  `memory/projects/universal-core/canon-identity/the-custodian-role.md`. Draft for the template,
  0.44 violations per 100 words against a gate of 1.5. Not proposed: its mechanism does not exist.
- **The laboratory proposal answered**, in three passes, each superseding the last.

## Decided

- **Custodian is a role, not an agent.** Set by the Principal. Chief of Vibes is the agent and the
  only one with access to the role. Both `state.md` files put the role name in the `agent` field,
  which is how one agent ended up with two vaults.
- **The canon is everyone's workspace.** It exists to receive what copies prove. Nobody edits it
  directly because a direct edit reaches no review, no version and no copy, not because anybody is
  unwelcome. Everyone comes through the same door, the agent standing on the canon included.
- **Custody is the role's and never ships.** `.canon` conflates three facts: custody, parent, and
  root. The template carries no custody marker.
- **The charter does not ship before its mechanism.** A document describing a control that is not
  built is the defect already recorded twice here.

## Lessons

- **A branch is not a repository.** This session read a superseded `state.md` for its whole length
  because nothing checked `git ls-remote --heads` for a second vault. It then reported two
  documented backlog items as discoveries and wrote a plan against a tree two releases stale.
- **A test that pins a wall-clock offset has an expiry date.** It reads as correct for months.
- **The second hop has no sensor.** Hop one is checked at session start and skipped on the canon by
  construction, so nothing watches the branch where the work happens.
- **Three verdicts in one thread, each right about the mechanism and wrong about the frame.** The
  frame came from the Principal every time. Measuring a proposal against the specification as
  written cannot find an error in the specification's own model.
