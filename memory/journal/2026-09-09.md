---
day: 09-09-2026
---

# 09-09-2026

## Done

- **#93 and #94 merged in order**, 1.81.0 and 1.82.0. Both sides at 1.82.0, 20 of 20 benches green.
- **The sync overwrote this vault**, and the release it was carrying is the one that forbids it.
  `state.md`, `backlog.md` and `corrections.md` took `main`'s empty forms. Restored from the
  pre-merge commit, byte-identical, nothing lost.
- **#95 opened**: the sync now runs the copy of itself that `main` carries, so a release governs the
  operation that installs it. Plus the shape sensor learning that a shipped placeholder is the shape.

## Lessons

- **A release is not tested by the mechanism it changes.** 1.81.0 shipped green and broke three
  things, each found by running the system: the clock, the two-vault sensor, and the vault itself.
- **The install path is a mechanism too.** Every rule about what sync does was written as though the
  sync applying it already had it. That is true from the second sync onward and never for the first.
- **The restore is the report.** Fixing it quietly would have left the next agent believing a merge
  cannot eat a vault.
- **A fourth tool read presence instead of a value.** `sweep-branches.sh` protected two dead release
  branches, because the empty form they inherited looked like an identity. Its bench passed the whole
  time: **the bench built a filled form and the shipped one is empty.** Folded into #95.

## Decided

- **The blast radius of a shipped input is every tool that reads it.** Four asked whether an agent
  lives here; three were fixed with the release and the fourth was missed. §5 now lists all four, so
  the next tool that asks has somewhere to look.

## The approval, carried out

- **#95 and #96 merged.** Both sides at **1.84.0**, 21 of 21 benches green. **The vault survived
  this sync**: the handoff ran, `main`'s newer `sync.sh` did the merge, and `memory/` kept this
  branch's version. The rule that failed on its own installation worked on the next one.
- **The shape sensor went quiet** — the `.gitkeep` fix landed. One hygiene item left, the quarantine.
- **The sweep is still holding two dead branches**; 1.83.0 fixes the reader and the next dispatch
  will clear them.

## 1.85.0 — teaching is a tool

- **The Principal's instruction: make teaching a tool, for them and for me.** The second half is the
  one that matters. `tools/explain.sh` answers from the tree with an address, so the Principal can
  check an explanation rather than trust it, and **the agent stops explaining from memory.**
- **Silence is stated, never filled.** No match exits non-zero and says the tree does not carry it.
  That is the sentence I failed to produce when I rediscovered a month-old note.
- **It never paraphrases**, because a summary of a rule is a second copy of it one step away, and
  that is what drifts.
- Nine cases. The one that earns the tool: every line it prints exists at the address it gives.
- Pull request #97, and it closes the last of the seven tagged findings.

## Lessons

- **A duty with no function is a claim.** The post asserted teaching for weeks and the header field
  is what exposed it, because a session had to write `no function declared` while doing the thing.
- **The rule that fails on its own installation is not disproved by that.** 1.83.0's handoff could
  not save the sync that carried it and saved the one after. Ordering, not correctness.

## 1.85.0 landed

- **#97 merged.** Both sides at **1.85.0**, 21 of 21 benches green, and the sync kept the vault
  again — twice now with the handoff in force.
- **The tool works where it lives.** `bash tools/explain.sh teach` answers from §3, both posts, the
  function and the bench, each with its line. That is the first exercise of the function that did
  not exist yesterday.
- **The sweep was re-dispatched** with the fixed reader on `main`, to clear the branches the old one
  protected.

## 1.86.0 — the first message begins onboarding

- **The Principal: nobody should have to ask for an agent.** Correct, and the gate was one clause in
  the skill's `description`: *and the user chooses to create an agent*. A description is the
  runtime's trigger, so that sentence was the machinery, not a note about it.
- **The menu asked a question its reader could not answer.** Somebody who has just arrived cannot
  pick between *create your agent* and *maintain the template*. §3 already calls that a defect.
- **The exceptions are the half that matters.** A canon that began onboarding would create an agent
  where none may live, and an undetermined repository would put a vault in the wrong place. The
  bench's risk cases are those two, not the copy.
- **The menu had never been benched at all** — the first thing a newcomer reads, unasserted. Eight
  cases now.
- Pull request #98.

## Lessons

- **I answered from the tree and was still wrong in the reply before this one.** I told the Principal
  the skill fires on its own; `tools/explain.sh` had given me the addresses and I did not open the
  `description` line it named. **The tool moves the labour of looking, and reading is still mine.**
  That is exactly what `functions/teach.md` says it cannot do, one day old and already demonstrated.

## 1.86.0 landed, and 1.87.0 — privileges

- **#98 merged**, both sides at 1.86.0. The first message now begins onboarding in a copy.
- **The Principal named the missing half: a post grants privileges, not only functions.** Correct,
  and the split is the same one 1.69.0 made: a function is work, a privilege is permission, and one
  word doing both jobs cannot produce a checkable sentence.
- **`enforced_by` is the field that earns the catalogue.** A grant written where nothing backs it
  reads like a control. `merge-the-template` is the honest case: nothing at the remote distinguishes
  an approved merge from one this agent decided on, and the entry says so.
- **`publish` is catalogued and granted to nobody.** Silence is where a future session reasons its
  way in.
- **The catalogue had never been indexed at all** — a gap in the file that claims to map where every
  rule lives.
- Pull request #99.

## 1.88.0 — the post held by an absence

- **The Principal's addition is what made it coherent**: the founding post *transforms* into
  steward rather than handing over, because an agent is reachable from any chat and there is
  nothing to hand to. Same session, same conversation, different post.
- **The header is what turns that into a reading.** §9's fourth field says `founder/onboard`, then
  `steward/<function>`, in one chat. Without it the transformation would be an assertion.
- **The hole was only visible after 1.87.0.** Privileges gave *may this session do that* an address,
  and the first thing the address exposed was a session doing more than any post grants while
  holding none.
- **`create-the-repository` is the honest entry**: its enforcement is almost nothing, and the rule
  doing the work is *private by default*, which no mechanism backs.
- Pull request #100, stacked on #99.

## Lessons

- **A catalogue's first use is finding what it does not contain.** Four privileges shipped, and the
  gap they exposed was the one nobody had thought to look for: the actor that exists before the
  actors do.

## Both landed

- **#99 and #100 merged in order.** Both sides at **1.88.0**, 21 of 21 green, vault intact through
  the sync for the third time running.
- **In force that was not this morning**: a post grants privileges as well as authorizing functions;
  four privileges name what enforces them and `publish` names that no post holds it; the catalogue
  is indexed; and the session that builds an agent holds `founder` rather than nothing, becoming
  `steward` when the form is filled.

## 5S · tree · defect hunt

- **The anchor read another repository's vault.** `cd "${CLAUDE_PROJECT_DIR:-.}"` was unguarded, so a
  failed cd left the hook measuring whatever tree it stood in. Reproduced: the header named a
  different agent, every field well formed. Same class as the two-vault incident — each reads
  correctly alone.
- **§8's growth test had no sensor**, so it was applied one release at a time and a run of thirteen
  added **+2724 words and +1046 lines** with nothing reporting it. `ready.sh` prints both against
  `main` now.
- **The hunt's negative half is the evidence it was a hunt**: no dead functions, no unreferenced
  tools, no `local x=$(…)` swallowing a status, no unguarded `read`. My first dead-code pass produced
  nine false positives from a naive grep, which is why the second one counted repo-wide.
- Pull request #101. Monitor spawned, verdict pending.

## Lessons

- **A rule applied per release is not applied to a run of them.** Nobody broke §8; thirteen releases
  each added a little of both, and the test has no window wider than one change.
- **A bench that exits non-zero and prints no failure line is the mute shape from the other side.**
  Two did it under parallel load and were green five times after. Environmental, and worth naming.
